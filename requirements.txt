fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0
python-docx>=1.1,<2.0
beautifulsoup4>=4.12,<5.0
lxml>=5.2,<6.0

# Only needed for cPanel/Passenger-style shared hosting, which runs Python apps as
# WSGI, not ASGI — see passenger_wsgi.py and README.md "Deploying on cPanel".
a2wsgi>=1.10,<2.0
